let Account = document.getElementById("calculate")
buttons= Array.from(document.querySelectorAll('button'));


button.map((buttons) =>{
    buttons.addEventListener('click', (event)=>{
        let key = event.target.innertext;
        if(key == "")
    })
})